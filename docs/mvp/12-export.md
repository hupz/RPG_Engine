# Export

1. Fix validator **errors** (export gate may block on dirty projects — Phase H).
2. Export / download project JSON from Editor save/export UI.
3. Ship JSON with the engine (`index.html` + data) or load via Editor **Open**.

Export is data-only: scenes, items, quests, UI, assets refs — no author JS required.

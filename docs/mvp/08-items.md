# Items & Rewards

1. **Items** tab → create item: name, category, description, icon, stackable.
2. Reward via macros or actions: `add_item`, `add_gold`, Loot Chest.
3. Item picker shows **Name (id)**.
4. Validator flags missing items and invalid amounts.

Uses existing inventory runtime — no second inventory system.

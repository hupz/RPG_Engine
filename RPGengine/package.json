{
  "name": "rpg-engine",
  "private": true,
  "version": "1.1.0",
  "description": "JSON-driven RPG engine + visual editor",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:engine": "node scripts/build.mjs engine",
    "build:editor": "node scripts/build.mjs editor",
    "build:all": "node scripts/build.mjs all"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
